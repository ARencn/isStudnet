预处理器
基于CSS的另一种语言
通过工具编译成CSS
添加了许多CSS不具备的特性
能提升CSS文件的组织

常见的 
       less(基于node用JS写的编译速度比较快 并且有一个浏览器中可直接运行的版本 不需要预先编译 入门简单 但是特殊特性会比较繁琐) 
       sass(编译比较慢)

CSS预处理器的功能:
        嵌套 反映层级和约束
        变量和计算  减少重复代码
        Extend和Mixin代码片段
        循环 适用于复杂又规律的样式
        import CSS文件模块化