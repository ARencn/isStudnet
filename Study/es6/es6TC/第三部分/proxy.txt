proxy:代理

        扩展(增强)对象的一些功能

        预警 上报 扩展功能 统计


        proxy是设计模式的一种  代理模式


   -------------------------

   语法:

                new proxy(target,handler);

                let obj=new Proxy(被代理对象,对代理对象做的操作)


                handler:
                {


                }
